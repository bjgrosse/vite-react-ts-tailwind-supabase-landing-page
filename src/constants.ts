const PROJECT_KEY = "project-key"; // TODO: change this to the actual project key
const PROJECT_NAME = "Project Name"; // TODO: change this to the actual project name

export { PROJECT_KEY, PROJECT_NAME };
